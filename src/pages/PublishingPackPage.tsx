import Card from "../components/Card";import type { PublishingPack } from "../types";
export default function PublishingPackPage({pack}:{pack:PublishingPack}){return <div className="page"><h1>Publishing Pack</h1><p className="lede">Copy/paste assets for posting and local promotion.</p><div className="grid two"><Card title="Facebook caption"><textarea readOnly value={pack.caption} rows={8}/></Card><Card title="Local group post"><textarea readOnly value={pack.localGroupPost??""} rows={8}/></Card></div><Card title="Posting notes"><p>{pack.postingNotes}</p><p><strong>Suggested time:</strong> {pack.suggestedPostTime}</p><p><strong>Hashtags:</strong> {pack.hashtags.join(" ")}</p></Card><Card title="Follow-up ideas"><ul className="plain-list">{pack.followUpIdeas.map(idea=><li key={idea}>{idea}</li>)}</ul></Card></div>}
